name: Tests
on: [push, pull_request]
jobs:
  unittest:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m unittest discover -s tests -v
      - run: python -m compileall -q thermaldeck
      - run: bash -n scripts/install.sh scripts/install-it87.sh
